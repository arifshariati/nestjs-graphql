version: "3"

services:
  db:
    image: postgres:latest
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: homelike
      POSTGRES_USER: homelike_user
      POSTGRES_PASSWORD: homelike_password